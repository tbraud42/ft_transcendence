# Network

DOMAIN_NAME=example.com

# API

API_PORT=3000

# Database

DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=example
DB_USERNAME=user
DB_PASSWORD=password

#ModSecurity
MODSEC_MODE = "DetectionOnly" # On dev | "On" on prod